volumes:
  data:

services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_PASSWORD=passwd
      - POSTGRES_USER=postgres
      - POSTGRES_DB=manga
    ports:
      - 5432:5432
    volumes:
      - data:/var/lib/postgresql